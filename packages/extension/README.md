# @tingly/extension

The shipped VS Code extension for `vscode-tingly-i18n`. Bundles `@tingly/core` plus the framework adapters.

Development is in this monorepo; do not consume this package outside the workspace.
